extends layout

block content
  h1= title
  p Welcome to #{title} Avinash chundu
  //- button(type='button',onclick='update()',class='btn btn-primary') Press Me
  //- div(id="target") text of Display 
  script.
    var client_count = 0;
    function update(){
      client_count +=1;
      document.getElementById("target").innerHTML = "Value now " + client_count;
    }
  //- a(href='earphones') Earphones
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/earphones") Take me to earphones
    br
    a(href="/logout") Logout